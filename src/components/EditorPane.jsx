import React from 'react';

import styles from './EditorPane.css';

export default () => (
  <div>
    <textarea className={styles.textarea}></textarea>
    <textarea className={styles.textarea}></textarea>
  </div>
);
